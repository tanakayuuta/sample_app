<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "つぶやきを入力" %>
    <%= f.file_field :image %>
  </div>
  <%= f.submit "投稿する", class: "btn btn-lg btn-primary" %>
<% end %>